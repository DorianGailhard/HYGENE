# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: meshPiano

dataset:
  name: meshPiano

training:
  batch_size: 10

validation:
  batch_size: 1

spectral:
  num_features: 2
