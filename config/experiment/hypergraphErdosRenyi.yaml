# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: hypergraphErdosRenyi

dataset:
  name: hypergraphErdosRenyi

training:
  batch_size: 64

validation:
  batch_size: 15

spectral:
  num_features: 2
