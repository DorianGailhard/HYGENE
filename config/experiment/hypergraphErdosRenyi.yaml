# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: hypergraphErdosRenyi

dataset:
  name: hypergraphErdosRenyi

training:
  batch_size: 32

validation:
  batch_size: 20

spectral:
  num_features: 0
