# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: hypergraphEgo

dataset:
  name: hypergraphEgo

training:
  batch_size: 16

validation:
  batch_size: 32

spectral:
  num_features: 0
