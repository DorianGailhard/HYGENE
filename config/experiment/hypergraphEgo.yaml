# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: hypergraphEgo

dataset:
  name: hypergraphEgo

training:
  batch_size: 10

validation:
  batch_size: 10

spectral:
  num_features: 2
