# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: hypergraphTree

dataset:
  name: hypergraphTree

training:
  batch_size: 16

validation:
  batch_size: 16

spectral:
  num_features: 0
