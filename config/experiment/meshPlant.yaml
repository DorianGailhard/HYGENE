# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: meshPlant

dataset:
  name: meshPlant

training:
  batch_size: 10

validation:
  batch_size: 6

spectral:
  num_features: 2
